apiVersion: v1
kind: Service
metadata:
  name: nodeapp-svc
  namespace: sandbox
  labels:
    app: nodeapp
    environment: sandbox
spec:
  ports:
    - port: 8080
      protocol: TCP
      targetPort: 8080
  selector:
    app: nodeapp
    environment: sandbox
  type: ClusterIP
